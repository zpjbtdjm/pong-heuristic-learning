# AGENTS.md

## Environment

- Before starting or running project commands, activate the conda environment named `HL`.
- Use:

```bash
conda activate HL
```

## Dependencies

- Install any new Python packages with `pip`.
- Use:

```bash
pip install <package-name>
```

